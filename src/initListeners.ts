import { onChangeListener } from "./Listeners/onChangeListener"
import { onClickListener } from "./Listeners/onClickListener"

export const initListeners =()=>{

    onClickListener()
    onChangeListener()
}