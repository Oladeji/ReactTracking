import { DataElm } from "./types";

const DATA_ROOT = 'data-analytics';
const NAME_ROOT='analytics'

export const DATA_ONCLICK : DataElm= {
    attr: `${DATA_ROOT}-onclick`,
    name:`${NAME_ROOT}Onclick`,
}

export const DATA_ONCHANGE : DataElm= {
    attr: `${DATA_ROOT}-onchange`,
    name:`${NAME_ROOT}onchange`,
}